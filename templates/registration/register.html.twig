<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/adminindex.css') }}">
    <style>
        body {
            background-color: #e6f7ff;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .card {
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            background-color: #ffffff;
        }
        .card-header {
            background-color: #007bff;
            color: #fff;
            text-align: center;
            padding: 1.5rem;
        }
        .btn-primary {
            background-color: #007bff;
            border: none;
            width: 100%;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        .mt-3 {
            margin-top: 1.5rem;
        }
        .sign-in-link {
            margin-top: 10px;
            text-align: center;
        }
        .sign-in-link a {
            color: #007bff;
            text-decoration: none;
            transition: color 0.3s;
        }
        .sign-in-link a:hover {
            color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                <p class="h4">USER REGISTER</p>
            </div>
            <div class="card-body">
                {{ form_start(registrationForm) }}
                <div class="mb-3">
                    {{ form_row(registrationForm.username, {'label': 'Username', 'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(registrationForm.first_name, {'label': 'First Name', 'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(registrationForm.last_name, {'label': 'Last Name', 'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_row(registrationForm.password, {'label': 'Password', 'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary">Register</button>
                </div>
                {{ form_end(registrationForm) }}
                <div class="sign-in-link">
                    <p>Already have an account? <a href="{{ path('app_login') }}">Log In</a></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
